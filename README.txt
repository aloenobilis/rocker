rocker = React + Docker

"🌩 From the kernel to the cloud 🌩" is a presentation about OS internals, AWS offerings and Containers.

This is a ReactJS App that uses SpecticleJS (presentation package), it runs
within a Docker container hosted on AWS Elastic Beanstalk. The diagrams were created using Balsamiq Wireframes.

Developer Start: docker-compose -f docker-compose-dev.yml up --build

Amazon Web Services :)
